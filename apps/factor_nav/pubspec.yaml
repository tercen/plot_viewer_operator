name: factor_nav
description: Tercen factor navigator — browse and drag data factors from workflow steps.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  get_it: ^8.0.3
  font_awesome_flutter: ^10.8.0
  web: ^0.5.1
  sci_tercen_client:
    path: /home/thiago/workspaces/tercen/main/sci_tercen_client/sci_tercen_client

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
