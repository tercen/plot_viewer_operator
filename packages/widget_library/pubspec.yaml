name: widget_library
description: Shared widgets and theme for the Tercen PWA monorepo.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0
  flutter: '>=3.5.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  font_awesome_flutter: ^10.8.0

flutter:
  uses-material-design: true
